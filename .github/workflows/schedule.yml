name: rolling
permissions:
  contents: read
on:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron:  '7 7 * * *'
jobs:
  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: EmbarkStudios/cargo-deny-action@v1
